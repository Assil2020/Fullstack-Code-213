<!DOCTYPE html>
<html lang="en">
  <head> </head>
  <body>
    <h1>Shopping Cart</h1>

    <% if (addedToCart) { %>
    <p>Book successfully added to your cart!</p>
    <% } %> <% if (cart.length === 0) { %>
    <p>Your cart is empty.</p>
    <% } else { %>

    <a href="/shop">Back to Shop</a>
    <% } %>
  </body>
</html>
